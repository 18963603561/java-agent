# 全局工作约定（必须遵守）

> 本约定适用于所有解释、分析、设计、编码、评审、文档与自动化产出内容。  
> 所有参与者（人类与智能体）必须严格遵守。

---

## 【语言】

1) 所有解释、分析、步骤、设计说明、文档内容、评审结论，统一使用中文（简体）。  
2) 如需引用英文原文（报错、协议字段、第三方文档），请保留英文，并在下一行给出中文解释。  
3) 除代码和英文原文外，不允许中英混排。  

## 【代码注释】

1) 类名、方法名、变量名、包名、文件名、配置 key 必须使用英文，并遵循现有命名风格。  
2) 所有新增/修改的 Java 注释（类注释、方法注释、 JavaDoc、行内注释）必须使用中文（简体）。  
3) 重要类、重要方法、关键属性、bean 类对象和 bean 属性必须补充注释，描述用途、输入输出、边界条件与注意事项。  
4) 类私有变量名  全局变量名 必须增加注释并描述用途。
5) 方法体中文注释硬性规则（必须执行，可验收）：

   5.1) 强制覆盖范围：任何新增/修改的方法体中，出现以下结构必须在其**上一行**添加一条中文注释（不得用块注释替代）：

   ```text
   if / else if / else
   for / while / do-while
   try / catch / finally
   return（非简单直返，含条件或加工时）
   调用子方法 / 工具方法 / 第三方 SDK / 外部接口（含 HTTP / DB / MQ / 文件等）
   抛异常 / 异常转换 / 吞异常相关逻辑
   ```

   5.2) 注释内容格式（一行内写清楚，禁止空泛）：
   - 对分支：说明“判断目的 + 条件含义 + 走该分支会做什么”。  
   - 对循环：说明“循环目标 + 迭代对象含义 + 退出条件/风险点”。  
   - 对方法调用：说明“调用目的 + 关键入参含义 + 失败/返回为空/重试策略”。  
   - 对异常：说明“异常场景 + 影响范围 + 处理策略（继续/降级/终止）”。

   5.3) 逐行对应要求：
   - 每个结构块至少 1 行注释，且注释必须紧贴该结构块（上一行）。  
   - 复杂块（≥3 个分支或 ≥2 层嵌套）需要额外补充 1 行说明“设计意图/风险点”。

   5.4) 验收与拒收条件（必须执行）：
   - 输出前必须自检：列出本次方法体中出现的 `if/for/try/调用/异常` 数量，并声明“已逐一补齐对应中文注释”。  
   - 若任意一个结构缺少对应注释：视为不合格输出，必须重写补齐后再提交。

   5.5) 编码要求：所有新增/修改注释必须为中文（简体），文件编码 UTF-8（无 BOM），避免乱码。

6) 代码中文注释不可乱码情况，要求 UTF-8（无 BOM）。

---

## 【日志与可观测性（必须落地）】

1) 对以下位置必须增加日志（使用项目现有日志框架与格式）：
- 外部接口调用（HTTP / DB / MQ / 文件系统）  
- 核心业务关键路径（开始、关键分支、结束）  
- 异常捕获处（必须带上下文信息与异常堆栈）

2) 日志级别建议：
- debug：调试细节（参数、分支信息、耗时）  
- info：关键业务流程节点（开始/结束/结果摘要）  
- warn：可恢复问题或非预期但可继续执行的情况  
- error：不可恢复错误或导致流程失败的情况

3) 对容易出错或逻辑复杂的代码块，必须增加中文注释说明设计意图与风险点。

---

## 【编码与文件格式】

1) 保存文件为 UTF-8（无 BOM）。  
2) 换行统一使用 LF。  
3) 中文不要出现乱码情况。如：”???” “ä¸å”一定不能出现。

---

## 【文档落盘】

1) 所有需要形成文档的内容，默认写入 `doc/<YYYYMMDD>/` 目录（不存在则创建）。  
2) 文档命名规则：`<主题-子主题>-<YYYYMMDDHHmm>.md`  
3) 临时记录、调研笔记、对话产物写入：`doc/<YYYYMMDD>/notes/`  
4) 除非我明确指定路径，禁止将文档写到项目根目录或其它目录。  
5) 任何超过 30 行的说明内容，不要直接输出在对话中，必须写入 `doc/<YYYYMMDD>` 下的 `.md` 文件。

---

## 【反馈】

1) 每次写文档后，请在回复中列出：
- 新增/更新的文件路径  
- 每个文件的一句话用途说明
