# 全局工作约定（必须遵守）

> 本约定适用于所有解释、分析、设计、编码、评审、文档与自动化产出内容。  
> 所有参与者（人类与智能体）必须严格遵守。

---

## 【语言】

1) 所有解释、分析、步骤、设计说明、文档内容、评审结论，统一使用中文（简体）。  
2) 代码中的类名、方法名、变量名、包名、文件名、配置 key 必须使用英文，并遵循现有命名风格。  
3) 如需引用英文原文（报错、协议字段、第三方文档），请保留英文，并在下一行给出中文解释。  
4) 除代码和英文原文外，不允许中英混排。  
5) 所有新增/修改的 Java 注释（类注释、方法注释、JavaDoc、行内注释）必须使用中文（简体）。  
6) 重要类、重要方法、关键属性、bean类对象和bean属性必须补充注释，描述用途、输入输出、边界条件与注意事项。  

---

## 【日志与可观测性（必须落地）】

1) 对以下位置必须增加日志（使用项目现有日志框架与格式）：
- 外部接口调用（HTTP / DB / MQ / 文件系统）
- 核心业务关键路径（开始、关键分支、结束）
- 异常捕获处（必须带上下文信息与异常堆栈）

2) 日志级别建议：
- debug：调试细节（参数、分支信息、耗时）
- info：关键业务流程节点（开始/结束/结果摘要）
- warn：可恢复问题或非预期但可继续执行的情况
- error：不可恢复错误或导致流程失败的情况

3) 对容易出错或逻辑复杂的代码块，必须增加中文注释说明设计意图与风险点。

---

## 【编码与文件格式】

1) 保存文件为 UTF-8（无 BOM）。  
2) 换行统一使用 LF。  
3) 中文不要出现乱码情况。如：”???”一定不能出现。  
---

## 【文档落盘】

1) 所有需要形成文档的内容，默认写入 `doc/` 目录（不存在则创建）。  
2) 文档命名规则：`doc/<主题>-<YYYYMMDD>.md`  
3) 临时记录、调研笔记、对话产物写入：`doc/notes/`  
4) 除非我明确指定路径，禁止将文档写到项目根目录或其它目录。  
5) 任何超过 30 行的说明内容，不要直接输出在对话中，必须写入 `doc/` 下的 `.md` 文件。  

---

## 【反馈】

1) 每次写文档后，请在回复中列出：
- 新增/更新的文件路径  
- 每个文件的一句话用途说明  

示例：
- `doc/mcp-design-20260122.md`：记录 MCP 工具注册与调用流程设计说明  
- `doc/notes/debug-record-20260122.md`：记录本次调试过程与问题结论  
