# runtime 第三阶段（I/J/K）验收记录（2026-02-07 18:35）

## 1. 验收范围

- 验收依据：`doc/runtime第三阶段整改任务清单-202602071635.md`
- 验收阶段：
  - 任务 I：收敛动态边界（类型化对象）
  - 任务 J：扩展原始存储抽象（写入 + 读取/解析）
  - 任务 K：核心链路切换与清理（去兼容分支 + 防回退）
- 验收目标：
  - 新项目按新方案直接落地，不保留低价值历史兼容分支。
  - 核心链路减少裸 `Map` 键依赖，沉淀类型化入口。
  - `RawResultResolveService` 从“依赖具体实现”升级为“依赖抽象路由”。

---

## 2. 阶段落地结果

## 2.1 任务 I 落地结果（通过）

### 关键新增

- `src/main/java/com/example/agent/runtime/model/input/ApprovalInput.java`
  - 新增审批类型对象，统一 `requiresApproval/approvalSource` 解析。
- `src/main/java/com/example/agent/runtime/model/input/StepInputView.java`
  - 新增步骤输入类型化视图，统一执行输入构建入口。
- `src/main/java/com/example/agent/runtime/model/input/LastStepInput.java`
  - 新增“上一步快照”对象，承载高频上下文字段。

### 关键改造

- `src/main/java/com/example/agent/runtime/model/StepSpec.java`
  - 增加 `approvalInput()`、`toInputView(...)`，并由 `toExecutionInput()` 复用类型化视图。
- `src/main/java/com/example/agent/runtime/step/RuntimeContext.java`
  - 增加 `get/setApprovalInput`、`get/setLastStepInput`、`get/setSteps` 等类型化访问。
- `src/main/java/com/example/agent/runtime/step/contract/StepExecutionRequest.java`
  - 增加 `StepInputView` 字段与 `getStepInputView()`。
- `src/main/java/com/example/agent/runtime/step/contract/StepExecutionOutput.java`
  - 增加高频 typed getter：`getAnswerText/getMode/getToolStatus/getDecisionRawRef/getSummaryRawRef`。
- `src/main/java/com/example/agent/runtime/engine/StepExecutionCoordinator.java`
  - 审批门禁改为传递 `StepInputView`。
- `src/main/java/com/example/agent/runtime/control/RuntimeApprovalGate.java`
  - 改为优先消费 `StepInputView + ApprovalInput`，收敛审批判断逻辑。
- `src/main/java/com/example/agent/runtime/react/ReactLoopService.java`
  - ReAct 审批请求接入 `StepInputView`，并回填 `RuntimeContext` 审批输入。
- `src/main/java/com/example/agent/runtime/engine/RuntimeContextUpdateService.java`
  - 使用 `LastStepInput` 写回运行上下文，移除 `promoteApprovalFields(...)` 提升逻辑。

### 任务 I 测试补齐

- 新增：
  - `src/test/java/com/example/agent/runtime/StepInputViewTest.java`
  - `src/test/java/com/example/agent/runtime/RuntimeContextTypedAccessTest.java`
- 修改：
  - `src/test/java/com/example/agent/runtime/RuntimeContextUpdateServiceTest.java`
  - `src/test/java/com/example/agent/runtime/StepExecutionOutputTest.java`
  - `src/test/java/com/example/agent/runtime/engine/StepFailureRecoveryServiceTest.java`

---

## 2.2 任务 J 落地结果（通过）

### 抽象升级

- `src/main/java/com/example/agent/runtime/raw/store/RawResultStore.java`
  - 扩展为读写一体抽象：新增 `storeType()`、`supports(...)`、`loadByRefId(...)`、`loadByStoreId(...)`。
- `src/main/java/com/example/agent/runtime/raw/store/RawResultStoreRegistry.java`
  - 新增存储注册路由，统一按 `RawStoreType`/`refId` 路由读取。
- `src/main/java/com/example/agent/runtime/raw/store/FileRawResultStore.java`
  - 新增文件模式存储实现，统一 `file/txt` 读取语义。

### 既有实现统一

- `src/main/java/com/example/agent/runtime/raw/store/InMemoryRawResultStore.java`
- `src/main/java/com/example/agent/runtime/raw/store/RedisRawResultStore.java`
- `src/main/java/com/example/agent/runtime/raw/store/SizeAwareRawResultStore.java`

以上三类均补齐 `storeType/loadByRefId/loadByStoreId`。

### 解析服务改造

- `src/main/java/com/example/agent/runtime/raw/RawResultResolveService.java`
  - 删除对具体实现类 `ObjectProvider` 的依赖。
  - 改为依赖 `RawResultStoreRegistry` 抽象路由。
  - 删除 legacy `raw:` 分支处理，统一要求 `rawref` 或 file public url。

### API 统一下载入口

- `src/main/java/com/example/agent/api/http/controller/RawResultController.java`
  - 新增 `GET /api/v1/raw/download?ref=`。
  - 控制器不再感知存储类型，统一走 `resolve(ref)`。

### 任务 J 测试补齐

- 新增：
  - `src/test/java/com/example/agent/runtime/raw/RawResultStoreRegistryTest.java`
- 修改：
  - `src/test/java/com/example/agent/runtime/raw/RawResultResolveServiceTest.java`
  - `src/test/java/com/example/agent/runtime/raw/InMemoryRawResultStoreTest.java`
  - `src/test/java/com/example/agent/runtime/raw/RedisRawResultStoreTest.java`
  - `src/test/java/com/example/agent/runtime/raw/SizeAwareRawResultStoreTest.java`
  - `src/test/java/com/example/agent/gateway/controller/RawResultControllerTest.java`

---

## 2.3 任务 K 落地结果（通过）

### 核心链路清理

- 移除执行器/恢复链路中零散 `step.toExecutionInput()` 依赖，改走类型化视图：
  - `src/main/java/com/example/agent/runtime/recovery/StepFailureRecoveryService.java`
  - `src/main/java/com/example/agent/runtime/step/executor/ToolStepExecutor.java`
  - `src/main/java/com/example/agent/runtime/step/executor/ThoughtTreeStepExecutor.java`
  - `src/main/java/com/example/agent/runtime/step/executor/DebateStepExecutor.java`
  - `src/main/java/com/example/agent/runtime/step/executor/ResearchStepExecutor.java`

### 防回退治理

- 新增架构守门测试：
  - `src/test/java/com/example/agent/runtime/raw/RawResultResolveArchitectureTest.java`
  - 校验 `RawResultResolveService` 不允许回退依赖具体 store 类。
- 强化键访问守门：
  - `src/test/java/com/example/agent/runtime/MapKeyAccessGuardTest.java`
  - 新增第三阶段关键键规则，并为“权威解包点”配置白名单，防止误伤。

---

## 3. 验收标准映射

### 标准 I：核心链路类型化入口建立并被调用

- 结果：**通过**。
- 证据：`StepSpec/RuntimeContext/StepExecutionRequest/StepExecutionOutput` 均新增类型化入口，且编排链路已切换。

### 标准 J：原始结果解析不再依赖具体存储实现

- 结果：**通过**。
- 证据：`RawResultResolveService` 仅依赖 `RawResultStoreRegistry`，新增 `RawResultResolveArchitectureTest` 持续守护。

### 标准 K：历史兼容路径清理并具备防回退机制

- 结果：**通过**。
- 证据：legacy `raw:` 分支已删除；新增架构守门与键访问守门测试。

---

## 4. 分阶段全流程测试记录

## 4.1 任务 I 完成后

- 命令：`mvn -q -DskipTests compile`
- 命令：`mvn test -q`
- 结果：通过。

## 4.2 任务 J 完成后

- 命令：`mvn -q -DskipTests compile`
- 命令：`mvn test -q`
- 结果：通过。

## 4.3 任务 K 完成后

- 命令：`mvn -q -DskipTests compile`
- 命令：`mvn test -q`
- 结果：通过（最终回归）。

补充核验：

- 已检查 `target/surefire-reports`，无 `Failures: [1-9]` / `Errors: [1-9]` 命中。

---

## 5. 结论

- 第三阶段 I/J/K 已完成落地并通过验收。
- 本轮遵循“新项目无历史包袱”约束：
  - 删除低价值 legacy 分支；
  - 统一抽象与路由；
  - 强化防回退测试。
- 当前状态满足进入后续功能迭代前的架构稳定门槛。

