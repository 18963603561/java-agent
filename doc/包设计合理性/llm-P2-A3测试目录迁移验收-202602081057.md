# LLM P2-A3 测试目录迁移验收

## 1. 目标

按“每阶段全量测试后再进入下一步”的要求，完成 LLM 测试目录从 `src/test/java/com/example/agent/model` 向按域分层目录迁移。

---

## 2. 分阶段迁移结果

## 阶段1：`provider` / `tooling`

迁移文件：

1. `DefaultModelProviderTest`
2. `LocalFallbackStrategyTest`
3. `ModelFallbackPolicyTest`
4. `ModelRouterTest`
5. `ModelToolResolverTest`
6. `ToolingContextMapperTest`

目标目录：

1. `src/test/java/com/example/agent/capabilities/llm/provider`
2. `src/test/java/com/example/agent/capabilities/llm/tooling`

验证：

1. `mvn -q -DskipTests compile` 通过。
2. `mvn test -q` 全量通过。

## 阶段2：`prompt` / `repair`

迁移文件：

1. `DefaultPromptAssemblerLegacyPathNoLeakTest`
2. `DefaultPromptAssemblerTest`
3. `DefaultPromptTemplateTest`
4. `PromptTemplateSnapshotLeakTest`
5. `JsonOutputRepairServiceTest`

目标目录：

1. `src/test/java/com/example/agent/capabilities/llm/prompt`
2. `src/test/java/com/example/agent/capabilities/llm/repair`

验证：

1. `mvn -q -DskipTests compile` 通过。
2. `mvn test -q` 全量通过。

## 阶段3：`client` / `architecture`

迁移文件：

1. `ModelInvocationServiceTest`
2. `LlmEventPayloadMapperTest`
3. `LlmArchitectureGuardTest`

目标目录：

1. `src/test/java/com/example/agent/capabilities/llm/client`
2. `src/test/java/com/example/agent/capabilities/llm/architecture`

验证：

1. `mvn -q -DskipTests compile` 通过。
2. `mvn test -q` 全量通过。

---

## 3. 结论

P2-A3（测试目录语义迁移）已完成，且每阶段均执行全量测试并通过，满足“分阶段推进 + 阶段门禁测试”要求。

