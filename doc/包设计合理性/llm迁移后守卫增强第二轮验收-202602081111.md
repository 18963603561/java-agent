# LLM 迁移后守卫增强第二轮验收

## 1. 目标

在“迁移后清理与收口”基础上，进一步增强测试目录治理能力，补充“测试类命名与目录映射一致性”检查，减少后续回流概率。

---

## 2. 改造内容

更新文件：

1. `src/test/java/com/example/agent/capabilities/llm/architecture/LlmArchitectureGuardTest.java`

新增能力：

1. 引入测试命名规则集（`TestNameRule`）。
2. 增加 `llmTestNameShouldMatchDomainDirectory` 测试：
   - `Architecture*` -> `architecture`
   - `*Prompt*` -> `prompt`
   - `*Repair*` -> `repair`
   - `*Tooling*` / `*ModelTool*` -> `tooling`
   - `*Provider*` / `*ModelRouter*` / `*ModelFallback*` / `*LocalFallback*` / `*Ollama*` / `*OpenAi*` / `*ToolPayload*` -> `provider`
   - `*Invocation*` / `*LlmEvent*` / `*LlmClient*` / `*RawRef*` / `*Failure*` -> `client`

---

## 3. 验证结果

执行：

1. `mvn -q -DskipTests compile`：通过。
2. `mvn test -q`（全量）：通过。

结论：

1. 守卫规则已升级，目录语义约束更严格。
2. 规则生效且未引入回归问题。

