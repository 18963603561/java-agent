# executeLlmStep ? MCP ????? toolChoice=AUTO ????

## ????
- ?? `executeLlmStep` ??**?????????** MCP ???
- MCP ?????????????/????????????? `executeLlmStep` ???
- `toolChoice=AUTO` ?? **?? `ModelToolResolver.applyTooling`** ??????`FinalOutputService` ??????????? LLM ???????????

## ????
- `toolChoice.mode = AUTO`
- ???? `AgentRuntime.executeLlmStep` ???? MCP ??

## ?????? MCP ???????
1) `AgentRuntime.run(...)`
- ???? `context` ? `toolChoice` ? `runtimeContext`?
- ?? `buildContextSnapshot(...)` ????????

2) `DefaultContextBuilder.build(...)`
- `buildToolState(...)` ?????????
- ???? `toolCatalog.listSummaries(...)`?

3) `DefaultToolCatalog.listSummaries(...)`
- ??? `refreshRemoteTools(...)`?
- ?? `McpToolSyncService.refreshIfNeeded(...)`?

4) `McpToolSyncService.refreshIfNeeded(...)`
- ???? TTL?????????????
- ?? `McpToolClient.listToolsRemoteOnly(...)`?
- ??????? `ToolRegistry`??? `mcp:<serverId>`????????????

5) `ToolState` ?? `ContextSnapshot`
- ?? `availableTools`????????????????

## executeLlmStep ?????
- `AgentRuntime.executeLlmStep(...)` ??? `FinalOutputService.finalizeOutput(...)`?
- `FinalOutputService` ?? `ModelRequest` ????? `ModelInvocationService`?
- **??**?? `ModelToolResolver.applyTooling(...)`?????? MCP ?????????
- ???????? `ToolRegistry`?? LLM ???????????????

## toolChoice=AUTO ???????????
- `ModelToolResolver.applyTooling(...)` ??? `TaskRequest.toolChoice`?
- ???????????????
  - `PlannerService.tryLlmPlan(...)`
  - `ReactLoopService`
  - `ReflectionService`
  - `MultiAgentCoordinator`
- **???** `FinalOutputService` ? `executeLlmStep`?
- ???
  - `toolChoice=AUTO` ???????/??/React ??????????
  - **????**?? LLM ?????

## ??? LLM ??????? MCP ???
1) ????????????????? `executeLlmStep`?
2) LLM ????????? `ModelToolResolver`?
3) ????????? `executeLlmStep` ???**??????/??**???

## ?????????????
### ?? A?? FinalOutputService ?????????????
???? LLM ??????????????
??????
- `FinalOutputService`
  - ?????`ModelToolResolver`
  - ? `finalizeOutput(...)` ??? `modelToolResolver.applyTooling(request, taskRequest, null)`
- `AgentRuntime.executeLlmStep(...)`
  - ???????????
???
- ?????????????????
- ??????????????????????

### ?? B?? LLM ??????????? LLM ???
???? LLM ??????????????
??????
- ?? `LlmStepService` ??? `FinalOutputService`?
  - ?? `ModelRequest` ??????
  - ?????????????
  - ?? `ToolExecutor` / `McpToolClient` ?????
  - ????????????????????
- `AgentRuntime.executeLlmStep(...)`
  - ?????????

### ?? C????????????????
????? LLM ????????????TOOL/REACT??????
??????
- `PlannerService.buildHeuristicPlan(...)`
  - ? `toolChoice=AUTO` ? `ToolState.availableTools` ????????? `REACT` ??????
- `PlannerService.tryLlmPlan(...)`
  - ?? `ModelToolResolver.applyTooling(...)` ??????????????????????

## ??? MCP ?????????
- MCP ????? `DefaultToolCatalog -> McpToolSyncService` ?? `ToolRegistry`?
- ????????????/??/React???????? `executeLlmStep`?

## ????
- ?????LLM ?????????????????? B?
- ????????????????????? C ?????
