# `FinalOutputService` ????????

## ???????
```
Exception in thread "task-exec-1" java.lang.StringIndexOutOfBoundsException: begin 0, end 786, length 111
	at java.base/java.lang.String.checkBoundsBeginEnd(String.java:4601)
	at java.base/java.lang.String.substring(String.java:2704)
	at com.example.agent.runtime.FinalOutputService.truncateSummary(FinalOutputService.java:376)
	at com.example.agent.runtime.FinalOutputService.resolveStepSummaryData(FinalOutputService.java:330)
	at com.example.agent.runtime.FinalOutputService.buildStepSummaries(FinalOutputService.java:289)
	at com.example.agent.runtime.FinalOutputService.buildFinalPrompt(FinalOutputService.java:222)
	at com.example.agent.runtime.FinalOutputService.finalizeOutput(FinalOutputService.java:123)
```
???????????? `substring` ?????????????????

## ????
- `agent.final-output.prompt-summary-max-chars` ?????????
- `resolveStepSummaryData` ?? `truncateSummary`??????????????????

## ????
- ?????`src/main/java/com/example/agent/runtime/FinalOutputService.java:361`?
- ?????`truncateSummary` ??????? `text.length()` ??????? `maxChars`??? `maxChars` ?????????? `substring`??????

## ????
### ???????
? `truncateSummary` ???????????????????????
```java
private String truncateSummary(String text) {
    if (!StringUtils.hasText(text)) {
        return text;
    }
    int maxChars = resolvePromptSummaryMaxChars();
    if (maxChars <= 0) {
        return text;
    }
    if (text.length() <= maxChars) {
        return text;
    }
    if (maxChars <= SUMMARY_TRUNCATED_SUFFIX.length()) {
        return text.substring(0, maxChars);
    }
    int endIndex = maxChars - SUMMARY_TRUNCATED_SUFFIX.length();
    if (endIndex <= 0) {
        return text.substring(0, maxChars);
    }
    return text.substring(0, endIndex) + SUMMARY_TRUNCATED_SUFFIX;
}
```

### ???????
?????????
```java
int safeEnd = Math.min(text.length(), maxChars);
```
???????????????? `safeEnd`?

## ??????
????????

```yaml
agent:
  final-output:
    prompt-summary-max-chars: 800
```

???????????????????????????????????????????????

## ???????
- ???????????????????????
- ?????`maxChars` ?????????????????????????
- ?????`FinalOutputServiceTest.finalizeOutputTruncatesSummaryInPrompt` ????????????????????

## ????
- ???????????????????????????
- ????????????????????????????
