# budget P4 落地实施记录

## 1. 实施背景

- 输入清单：`doc/budget-P4具体改造任务清单-202602091434.md`
- 目标问题：`P4` 问题 9（`cost` 子包单类孤岛，包粒度偏细）
- 实施原则：新方案直接落地，不保留历史兼容路径，不增加条件分支兜底。

## 2. 实施方案

- 目标结构采用单方案收敛：将成本计算能力并入 `token/pricing` 子域。
- 迁移原则：保持成本计算语义与方法签名稳定，仅调整包层级与依赖引用。
- 质量策略：通过契约测试 + 架构守卫测试固化结构优化结果。

## 3. 代码改造明细

### 3.1 成本子域迁移（P4-T02 ~ P4-T04）

- 新增成本计算器：
  - `src/main/java/com/example/agent/budget/token/pricing/CostCalculator.java`
- 调整调用引用：
  - `src/main/java/com/example/agent/budget/token/DefaultTokenUsageRecordFactory.java`
  - 从 `com.example.agent.budget.cost.CostCalculator` 切换到 `com.example.agent.budget.token.pricing.CostCalculator`
- 删除旧实现源码：
  - `src/main/java/com/example/agent/budget/cost/CostCalculator.java`

### 3.2 成本契约测试补齐（P4-T05）

- 新增成本计算器单测：
  - `src/test/java/com/example/agent/budget/CostCalculatorTest.java`
- 覆盖场景：
  - 模型为空返回 0
  - 零 token 返回 0
  - 大规模 token 场景
  - 费率精度场景
- 新增预算记录工厂契约单测：
  - `src/test/java/com/example/agent/budget/DefaultTokenUsageRecordFactoryTest.java`
- 覆盖场景：
  - 按模型费率自动计算成本
  - 当输入已提供 `costUsd` 时优先采用输入值

### 3.3 守卫规则补齐（P4-T06）

- 更新文件：
  - `src/test/java/com/example/agent/budget/BudgetArchitectureGuardTest.java`
- 新增守卫：
  - `budget/cost` 包不允许存在 Java 源码
  - `budget/token/pricing` 禁止依赖 `budget.trim` 与 `capabilities.context` 实现细节

## 4. 分阶段测试结果

### 4.1 阶段 A（结构迁移阶段）

- 执行：`mvn test`
- 首轮结果：`462 tests, 1 failure`
  - 失败原因：`budget.cost` 目录存在但无源码，守卫规则初版误判。
- 修复动作：守卫规则调整为“禁止存在 Java 源码文件”，而非“目录必须不存在”。
- 复跑结果：`462 tests, 0 failures`，通过。

### 4.2 阶段 B（契约与守卫阶段）

- 执行：`mvn test`
- 结果：`462 tests, 0 failures`，通过。
- 说明：新增成本契约测试与守卫规则在全量回归中稳定通过。

### 4.3 阶段 C（文档收口阶段）

- 执行：`mvn test`
- 结果：见本次收口回归结果（已在交付结论同步）。

## 5. 交付结论

- P4 清单核心任务已完成落地：
  - 成本计算能力已归并到 `token/pricing`。
  - 旧 `cost` 源码实现已删除。
  - 契约测试与架构守卫已补齐并生效。
- 预算域包结构更聚合，后续扩展费率策略时可在 `pricing` 子域集中演进。