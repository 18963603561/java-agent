# 返回结果结构 P0 改造任务清单

## 目标与范围
- 目标：补齐“原始快照输出开关”和“连续步骤编号”能力，保证结构说明与实际行为一致。
- 范围：仅 P0 任务，遵循现有包职责边界，不引入跨层耦合。

## P0 任务清单（按包职责拆分）

### 1. 原始快照输出开关文档与配置示例
- 所在包/模块
  - 配置：`com.example.agent.runtime.summary`（`StepSummaryProperties`）
  - 快照封装：`com.example.agent.runtime.raw.output`（`RawOutputEnvelopeBuilder`）
  - 文档：`doc/20260214/配置说明-202602140447.md`
- 任务内容
  - 在配置说明文档中增加 `agent.summary.rawEnable` 的用途、默认值、示例配置。
  - 补充“开启后 `steps[*].raw` 输出快照”的说明与限制（最大长度/字段数量）。
- 验收标准
  - 文档中出现明确的配置示例与解释。
  - 按示例配置启用后，`steps[*].raw` 不为 `null`。

### 2. 展示层连续步骤编号（stepIndex）
- 所在包/模块
  - 展示层或前端适配层（不修改运行时 `seq` 逻辑）。
- 任务内容
  - 在展示层根据当前 `steps` 列表生成 `stepIndex`（从 1 递增）。
  - 明确说明 `seq` 为事件序号，非连续编号，不在展示层复用。
- 验收标准
  - 前端展示连续编号且稳定。
  - `seq` 保持原义，未被重写或混用。

## 包设计合理性检查要点（P0 约束）
- 配置与运行逻辑分离：配置说明只落在 `doc`，不影响 `runtime` 代码职责。
- 展示层与运行时隔离：`stepIndex` 仅在展示层生成，不侵入 `runtime.step`。
- 不引入跨包依赖：不为 P0 需求新增跨层调用链或公共工具类。

## 影响文件清单（预期）
- 文档：`doc/20260214/配置说明-202602140447.md`
- 展示层文件：由前端/适配层确定（不改动后端运行时包）。

## 测试与验证
- 配置验证：启用 `agent.summary.rawEnable=true` 后检查 `steps[*].raw`。
- 展示验证：`stepIndex` 连续递增，`seq` 原样展示为事件序号。
