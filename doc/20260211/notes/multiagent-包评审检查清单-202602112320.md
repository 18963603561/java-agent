# multiagent 包评审检查清单-202602112320

## 1. 使用方式
- 适用范围：`src/main/java/com/example/agent/orchestration/multiagent`
- 使用时机：代码提交前自检、代码评审时逐项打勾。
- 判定原则：任何一项不满足均视为“需整改”，不得以历史原因豁免。

## 2. 分层与包边界检查
- [ ] 新增类是否放在正确子域（`role`、`usecase`、`dag`、`handoff`、`supervisor`）
- [ ] `dag.domain` 是否未依赖 `dag.infrastructure`
- [ ] `application` 是否仅依赖端口接口而非实现类
- [ ] `infrastructure` 是否未承载业务编排逻辑
- [ ] 是否未引入对 `api.http.controller` 的反向依赖

## 3. 可观测性检查
- [ ] 指标名是否统一使用 `MultiAgentMetricKeys`
- [ ] 标签键是否统一使用 `MultiAgentTagKeys`
- [ ] 事件 payload 键是否统一使用 `MultiAgentEventKeys`
- [ ] 关键日志是否包含 `workflowId`、`dagRunId`、`nodeId`（按场景）
- [ ] 异常日志是否带完整上下文与堆栈

## 4. 代码质量检查
- [ ] 关键方法是否有中文注释说明用途、输入输出、边界条件
- [ ] 方法体关键循环、分支、异常处理是否有中文注释
- [ ] 是否避免新增历史兼容分支
- [ ] 是否避免新增重复工具方法与重复字符串字典
- [ ] 命名是否与现有风格一致且语义清晰

## 5. 测试与回归检查
- [ ] 是否补充/更新对应单元测试
- [ ] 架构守护测试是否通过
- [ ] 阶段专项测试是否通过
- [ ] 全量回归测试是否通过

## 6. 文档一致性检查
- [ ] `multiagent/README.md` 与当前结构一致
- [ ] `multiagent/dag/README.md` 与当前分层一致
- [ ] `package-info.java` 与 README 不冲突
- [ ] 变更报告是否落盘到 `doc/20260211/`

