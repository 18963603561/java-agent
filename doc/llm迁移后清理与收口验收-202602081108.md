# LLM 迁移后清理与收口验收

## 1. 目标

在完成 P2-A3 测试目录迁移后，继续执行“迁移后清理与收口”，重点关注：

1. 清理旧 `com.example.agent.model` 下 LLM 测试残留。
2. 增加目录级防回流守卫，避免后续改动把 LLM 测试放回旧目录。
3. 按阶段要求执行完整全量测试。

---

## 2. 本轮改造

## 2.1 架构守卫补强

更新文件：

1. `src/test/java/com/example/agent/capabilities/llm/architecture/LlmArchitectureGuardTest.java`

新增约束：

1. `llmTestsShouldNotStayInLegacyModelPackage`：禁止 LLM 相关测试回流 `src/test/java/com/example/agent/model`。
2. `llmTestDirectoryShouldFollowDomainLayout`：约束 `src/test/java/com/example/agent/capabilities/llm` 仅允许 `architecture/client/prompt/provider/repair/tooling` 六类顶级子目录。
3. 同时校验包声明必须为 `com.example.agent.capabilities.llm.*`。

## 2.2 旧目录清理

已清理：

1. 删除空目录 `src/test/java/com/example/agent/model`。

---

## 3. 验证记录

执行：

1. `mvn -q -DskipTests compile`：通过。
2. `mvn test -q`（全量）：通过。

结论：

1. 本轮“迁移后清理与收口”已完成。
2. 目录与守卫规则形成闭环，可持续防止测试目录回流。

