# planning P3/P4 改造实施与验收

## 1. 目标与原则

- 对齐任务来源：`doc/planning-P3P4优化backlog-202602081939.md`
- 落地原则：仅增强不改行为，不引入历史兼容分支。
- 验收门禁：阶段结束执行全量测试 `mvn -q test`。

## 2. P3 落地内容

### 2.1 P3-OBS-01 解析错误类型统一化

- `PlanParseErrorTypes` 已作为 planning 解析错误词典。
- `PlanParser.parseAttempt`、`LlmPlanEngine`、trace 上报统一引用。

### 2.2 P3-OBS-02 规划链路耗时分段指标

- `PlannerService` 增加上下文映射、能力评估、LLM、fallback、总耗时日志。
- `LlmPlanEngine` 增加 prompt、bundle、tooling、parse、repair、总耗时日志。

### 2.3 P3-OBS-03 策略命中可观测增强

- `PlanningStrategyRegistry` 增加命中、未命中、终止、异常日志。

### 2.4 P3-TEST-01 `PlanParser` 健壮性测试增强

- `PlanParserTest` 新增参数化测试：空白、非法 JSON、缺失步骤、半结构化场景。
- 严格参数模式下增加多样错误类型断言。

### 2.5 P3-TEST-02 策略注册顺序稳定性

- `PlanningStrategyRegistryOrderingStabilityTest` 已覆盖随机注册顺序稳定性。

### 2.6 P3-TEST-03 不变式守卫

- `PlanningStrategyContextInvariantTest` 已覆盖防御拷贝、只读视图、受控追加接口。

### 2.7 P3-DOC-01 架构与扩展文档

- 新增 `doc/planning-架构与扩展说明-202602082006.md`。

### 2.8 P3-PERF-01 模板缓存可观测化

- `PlanningPromptBuilder` 增加模板缓存命中 debug 与初始化 info 日志。
- `PlanningPromptBuilderTest` 增加缓存复用测试。

## 3. P4 落地内容

### 3.1 P4-PERF-01 解析微基准

- 新增 `PlanParserMicroBenchmarkTest` 作为轻量微基准入口。
- 新增报告：`doc/planning-解析链路微基准与分配分析报告-202602082014.md`。

### 3.2 P4-PERF-02 大对象分配分析

- `PlanParser.parseInternal` 完成等价预分配优化（`steps/input` 容量预估）。
- 未改变解析语义与错误类型输出。

### 3.3 P4-GOV-01 包依赖守卫

- `PlanningArchitectureGuardTest` 已守卫 `strategy` 禁止越级依赖。
- 本轮扩展到 `capability` 包禁止依赖 `engine/telemetry`。

### 3.4 P4-GOV-02 上下文键治理清单

- 新增 `doc/planning-上下文键治理清单-202602082009.md`。
- `PlanningContextKeys.COGNITIVE_STRATEGY_LEGACY` 标记 `@Deprecated` 并给出替代键。

### 3.5 P4-TOOL-01 调试诊断工具化

- `PlanningDiagnosticsSnapshotBuilder` 已完成并具备测试覆盖。

### 3.6 P4-DOC-01 设计决策记录

- 新增 `doc/planning-设计决策记录-202602082011.md`。

## 4. 验收执行

### 4.1 全量测试

- 命令：`mvn -q test`
- 结果：通过

### 4.2 结论

- P3/P4 backlog 已按“仅增强不改行为”原则完成落地。
- 规划包在可观测性、测试完备度、治理规范与文档可追溯性方面达成阶段目标。

