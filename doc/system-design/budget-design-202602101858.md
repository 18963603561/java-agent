# `budget` 包系统设计说明

## 1. 包定位与核心功能
- 包定位：`budget` 管理 Token 成本、上下文预算分配、裁剪与压缩治理。
- 核心功能：
  - `TokenBudgetManager` 记录与统计用量。
  - `DefaultContextBudgetAllocator` 计算预算并产出分配状态。
  - `DefaultContextPruner` + `DefaultContextTrimmer` 两阶段上下文降载。
  - `ContextCompressionService` 在超阈值时触发压缩。
  - 发布预算事件与指标用于治理和观察。

## 2. 分层线框图
```mermaid
graph TB
    A[ContextSnapshot 输入] --> B[TokenBudgetManager]
    B --> C[ContextBudgetAllocator]
    C --> D[ContextPruner]
    D --> E[ContextTrimmer]
    E --> F[ContextCompressionService]
    F --> G[预算结果与报告]
    B --> H[TokenUsageRepository]
    G --> I[BudgetEventPublisher]
```

## 3. 关键流程图
```mermaid
graph TB
    A[收到上下文] --> B[构建 ContextBudgetRequest]
    B --> C[allocate 预算分配]
    C -->|ENABLED| D[prune 剪枝]
    C -->|DISABLED| H[跳过裁剪压缩]
    D --> E[trim 精细裁剪]
    E --> F{是否超阈值}
    F -->|是| G[compressIfNeeded]
    F -->|否| I[输出]
    G --> I
    H --> I
```

## 4. 状态流转图
```mermaid
graph TB
    S0[REQUEST_RECEIVED] --> S1[ALLOCATING]
    S1 --> S2[ENABLED]
    S1 --> S3[DISABLED_BY_REQUEST]
    S1 --> S4[DISABLED_BY_CONFIG]
    S1 --> S5[DISABLED_BY_DEPENDENCY]
    S2 --> S6[PRUNING]
    S6 --> S7[TRIMMING]
    S7 -->|超阈值| S8[COMPRESSING]
    S7 -->|未超阈值| S9[READY]
    S8 --> S9
    S3 --> S9
    S4 --> S9
    S5 --> S9
```

## 5. 类职责与设计原因
- `TokenBudgetManager`：统一成本台账，避免多点记账不一致。
- `DefaultContextBudgetAllocator`：聚焦预算分配计算，便于策略替换。
- `DefaultContextPruner`：先粗剪，降低精剪计算开销。
- `DefaultContextTrimmer`：按 section 插件化精剪，提升可维护性。
- `ContextCompressionService`：独立压缩链路，便于策略演进。

## 6. 关键类直接关系图
```mermaid
graph TB
    A[DefaultContextBudgetAllocator] --> B[ContextBudgetProperties]
    A --> C[TokenBudgetManager]
    D[DefaultContextTrimmer] --> E[ContextTrimHandler]
    D --> F[ContextTokenEstimator]
    G[ContextCompressionService] --> H[CompressionTriggerPolicy]
    G --> I[CompressionExecutionService]
    C --> J[TokenUsageRepository]
```

## 7. 重点说明
- `budget` 与 `capabilities.context` 联动决定上下文质量与成本平衡。
- 预算状态显式化提升问题定位效率。
- 所有图统一使用 `graph TB`。

