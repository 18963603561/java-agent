# `api` 包系统设计说明

## 1. 包定位与核心功能
- 包定位：`api` 是系统统一接入层，负责协议适配、鉴权协作、租户上下文注入和统一响应封装。
- 核心功能：
  - 提供任务、审批、预算、回放、时间线、调度、流式等 HTTP 接口。
  - 通过 `TenantContextFilter` + `TenantResolver` 落地多租户上下文。
  - 通过 `AuthService` 统一 API Key/JWT/可信上游鉴权。
  - 通过 Mapper 将 DTO 与领域命令解耦。
  - 通过全局异常处理器统一错误语义。

## 2. 分层线框图
```mermaid
graph TB
    A[客户端] --> B[TenantContextFilter]
    B --> C[Controller 层]
    C --> D[AuthService]
    C --> E[DTO/Mapper 层]
    E --> F[Orchestration/Runtime 门面]
    E --> G[Governance/Budget/History/Scheduler 服务]
    C --> H[GlobalExceptionHandler]
    F --> I[ApiResponse]
    G --> I
    H --> I
```

## 3. 关键流程图
```mermaid
graph TB
    A[HTTP 请求进入] --> B[过滤器解析租户]
    B --> C[Controller 解析参数]
    C --> D[AuthService.authenticate]
    D -->|通过| E[Mapper 转换命令]
    D -->|失败| X[异常映射 401/403]
    E --> F[调用下游服务]
    F --> G[返回结果并封装响应]
    F -->|异常| X
```

## 4. 状态流转图
```mermaid
graph TB
    S0[REQUEST_RECEIVED] --> S1[TENANT_RESOLVED]
    S1 --> S2[AUTHENTICATING]
    S2 -->|成功| S3[SERVICE_ROUTED]
    S2 -->|失败| S7[ERROR_MAPPED]
    S3 --> S4[SERVICE_EXECUTING]
    S4 -->|同步且终态| S5[RESPONSE_OK]
    S4 -->|异步或未终态| S6[RESPONSE_ACCEPTED]
    S4 -->|异常| S7
    S5 --> S8[RESPONSE_SENT]
    S6 --> S8
    S7 --> S8
```

## 5. 类职责与设计原因
- `TenantContextFilter`：统一租户上下文注入，避免控制器重复处理。
- `TaskController` 等控制器：仅负责协议层编排，保持领域逻辑下沉。
- `TaskHttpMapper`：集中转换规则，减少字段漂移。
- `GlobalExceptionHandler`：统一错误码与状态码映射。
- `ApiResponse`：统一返回结构，便于客户端处理。

## 6. 关键类直接关系图
```mermaid
graph TB
    A[TenantContextFilter] --> B[TenantResolver]
    C[TaskController] --> D[AuthService]
    C --> E[TaskHttpMapper]
    C --> F[TaskSubmissionService]
    C --> G[TaskQueryService]
    H[ToolApprovalController] --> D
    H --> I[ApprovalService]
    J[GlobalExceptionHandler] --> K[ErrorCodeException]
    J --> L[ApiResponse]
```

## 7. 重点说明
- `api` 与 `security`、`orchestration`、`runtime` 连接最紧密。
- 接入层负责把同步/异步差异转成稳定接口语义。
- 所有图统一使用 `graph TB`。

